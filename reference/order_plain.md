# Plain order function

A placeholder order function that returns the original order of given
features.

## Usage

``` r
order_plain(X, y)
```

## Arguments

- X:

  Input matrix, of dimension `nobs` by `nvars`; each row is an
  observation vector.

- y:

  Vector of response variables.

## Value

Sequence starting from 1 to `nvars`.
